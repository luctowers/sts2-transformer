# sts2-transformer

The goal is to build a transformer network that plays *Slay the Spire 2*
(Early Access), trained against a Python reimplementation of a subset of the
game's mechanics. Nothing is implemented yet — currently just scaffolding.

This is a personal learning/research project. It's very much a work in
progress and will probably never be "finished."
